#!/usr/bin/python3
def mul4(number):
    return number * 4
def multiply_list_map(my_list=[], number=0):
    if a_dictionary is None:
        return None
    new_dict = {}
    new_dict = map(mul4, my_set)