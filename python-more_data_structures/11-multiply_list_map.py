#!/usr/bin/python3

def multiply_list_map(my_list=[], number=0):
    if a_dictionary is None:
        return None
    new_dict = {}
    for key in a_dictionary:
        new_dict[key] = a_dictionary[key] * number
    return new_dict
